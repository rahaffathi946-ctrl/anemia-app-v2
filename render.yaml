services:
  - type: web
    name: anemiaai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # ← ستضع المفتاح يدوياً في لوحة Render
    plan: free
